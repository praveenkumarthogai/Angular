lazyloading an module will create separate chunk js file in dist folder which will not be part of index.html file

ng g interceptor [name]  creates an interceptor 


Map in Typescript

it like dictionary key and value pair data structure
let dictionary = new Map<any,any>()

dictionary.set(key,value);

dictionary.get(key);
